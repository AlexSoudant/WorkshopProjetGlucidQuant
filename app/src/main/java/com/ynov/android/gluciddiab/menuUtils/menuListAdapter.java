package com.ynov.android.gluciddiab.menuUtils;

/**
 * Created by admin on 02/04/17.
 */

public class menuListAdapter {//extends ArrayAdapter<ArrayList> {
/*

    private final Activity context;
    private final ArrayList textName;
    private final Button buttonDelete;

    public menuListAdapter(Activity context,
                           ArrayList textName, Button buttonDelete) {

        super(context, R.layout.item_panier, textName);
        this.context = context;
        this.textName = textName;
        this.buttonDelete = buttonDelete;

    }

    @Override
    public View getView(final int position, View view, ViewGroup parent) {
        LayoutInflater inflater = context.getLayoutInflater();
        View rowView = inflater.inflate(R.layout.item_panier, null, true);
        final TextView txtItem = (TextView) rowView.findViewById(R.id.panierNomItem);
        Button btnDelete = (Button) rowView.findViewById(R.id.panierButtonDelete);

        txtItem.setText(textName.get(0));

        btnDelete.setOnClickListener(new View.OnClickListener() {
            public void onClick(View v) {
                textName.remove(position);

            });

    });
    }
*/



}
